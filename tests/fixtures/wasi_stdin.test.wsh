---
stdout: |
  Got: hello
  Got: world
  Got: error!
---

echo -n hello | source $WASM_PATH/stdin.wasm
echo -n world | source $WASM_PATH/stdin.wasm
# Merge stderr to stdin
_error error! &| source $WASM_PATH/stdin.wasm
